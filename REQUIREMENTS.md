# Create notes on system requirements
echo "Required:
- C++ compiler with C++11 support minimum (C++17 recommended)
- CMake 3.15 or higher
- Python 3.6+ (for validation tests)
- Optional: CUDA toolkit (for xlnscuda integration later)